# Joining Hands practical lesson update

Upload these files/folders to the repository:

- `public/index.html` — replacement frontend
- `public/curriculum.json` — practical MS Word lessons
- `public/projects/word-project-1.png` through `word-project-6.png` — curriculum project images

The frontend expects the existing Node backend endpoints:
`/api/login`, `/api/me`, `/api/lesson-attempt`, `/api/attendance`, `/api/certificates`.

The project images are the exact images supplied in this conversation.
